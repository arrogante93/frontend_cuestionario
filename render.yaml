services:
  - type: static
    name: frontend-cuestionario
    env: static
    buildCommand: npm run build
    publishPath: dist/frontend_cuestionario/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
