services:
  - type: web
    name: frontend-cuestionario
    runtime: static
    buildCommand: npm run build
    staticPublishPath: dist/frontend_cuestionario/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
